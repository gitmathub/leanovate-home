---
layout: main.njk
theme: dark
---

{% import "partial.njk" as partial %}

<section class="centered">

  <h1 class="h3">{{ partial.getValueForKey(collections, tags, 'teaser', locale, 'white-paper')}}</h1>

  {{ partial.get(collections, tags, "teaser", locale) }}

  <img 
    src="{{ partial.getValueForKey(collections, tags, 'teaser', locale, 'teaser-image')}}"
    alt="teaser" class="centered">


<h3>{{ partial.getValueForKey(collections, tags, 'form', locale, 'headline') }}</h3>

{% include './form.njk' %}

</section>
